name: genpass
description: A password generator app written by Flutter/Dart.

version: 2.1.0+2010000

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  crypto: ^2.1.4
  shared_preferences: ^0.5.7+2
  provider: ^4.1.2
  logging: ^0.11.4
  tuple: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  fake_async: ^1.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/appicon.png
